name: birthday_card_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.3
  lottie: ^3.1.3
  confetti: ^0.7.0  # Add the confetti package version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
assets:
  - assets/balloons.json
  - assets/confetti.json
  - assets/fireworks.json